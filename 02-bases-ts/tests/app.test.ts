describe('Test in the app file', () => {

  // 1. Arrange >> Preparar lo que se va a probar

  // 2. Act

  // 3. Assert
  test('should be true', () => {
    expect(true).toBe(true);
  });

  // test('fire an error to test build not passing if test fails', () => {
  //   expect(30).toBe(40);
  // });
});